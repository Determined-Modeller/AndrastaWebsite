version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run verify:static-ready
        - npm run typecheck
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
